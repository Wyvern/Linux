sudo passwd root
su
sudo apt-get purge -y audit ufw rsyslog logrotate sysstat python3-problem-report sosreport apport watchdog popularity-contest   
sudo systemctl mask auditd.service systemd-journald.service systemd-journald.socket syslog.socket syslog.target 

echo 'LANGUAGE="en_US.UTF-8"' >> /etc/default/locale
echo 'LC_ALL="en_US.UTF-8"' >> /etc/default/locale
sudo locale-gen en_US.UTF-8
sudo dpkg-reconfigure tzdata

sudo apt-get install -y curl zsh tree htop deborphan nload dialog vim-enhanced

sh -c "$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)"

~/.zshrc
ZSH_THEME="ys"
plugins=(ubuntu sudo systemd git docker npm)

sudo add-apt-repository ppa:git-core/ppa

sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"
sudo apt-get update
sudo apt-get install docker-ce git
sudo usermod -aG docker $(whoami)

curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash -
sudo apt-get install nodejs

sudo chown -R $USER:$(id -gn $USER) /home/$USER/.config

sudo truncate -s 0 /var/log/*tmp
sudo chattr +i /var/log/*tmp
sudo ln -s /dev/null /var/log/alternatives.log

//PIP install
curl https://bootstrap.pypa.io/get-pip.py -o get-pip.py
python get-pip.py
pip install pipupgrade
pip install -U pip

//LLVM
wget -O - https://apt.llvm.org/llvm-snapshot.gpg.key|sudo apt-key add -

//Plex
echo deb https://downloads.plex.tv/repo/deb ./public main | sudo tee /etc/apt/sources.list.d/plexmediaserver.list
curl https://downloads.plex.tv/plex-keys/PlexSign.key | sudo apt-key add -
